# Pinned dependencies for reproducibility. For development, prefer
# `pip install -e .[dev]` which uses the broader ranges in pyproject.toml.
torch>=2.1,<3.0
transformers>=4.40,<5.0
datasets>=2.14
accelerate>=0.25
evaluate>=0.4
rouge-score>=0.1
nltk>=3.8
PyYAML>=6.0
tqdm>=4.65
numpy>=1.24
